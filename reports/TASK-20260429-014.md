# TASK-20260429-014 보고서 — 파이프라인 가이드/최종 Git 감사 보강

## 핵심 요약

- **상태:** 구현 및 로컬 검증 완료. 별도 smoke test는 TASK-20260429-015로 이어서 수행한다.
- **결론:** Generator 가이드의 stale 자동화 상태를 현재 구현과 맞췄고, TASK-010/011의 stale push metadata를 append-only `CORRECTION`으로 보정했다. `check-final-git-state.mjs`를 추가해 완료 Task의 pending push 표기를 감사에서 차단한다.
- **서비스 영향:** 제품 런타임 영향 없음. 하네스 운영 문서·감사 스크립트·원장 보정만 변경.
- **검증:** `npm run audit:harness`, `npm run check:encoding`, `git diff --check`, `node --check scripts/check-final-git-state.mjs`, `node scripts/check-final-git-state.mjs` PASS.
- **남은 리스크:** `audit:finalize`는 task 브랜치의 dirty 상태에서 의도적으로 FAIL한다. main 반영 후 clean/sync 상태에서 다시 실행해야 한다.
- **조치 필요:** TASK-015에서 실제 Generator → Validator-A smoke test를 수행한다.

## 변경 사항

| 영역 | 파일 | 내용 |
|---|---|---|
| 가이드 | `docs/guides/generator-pipeline-ko.md` | Validator-A 실행, retry 생성, Conflict Report 생성을 자동화된 부분으로 이동 |
| 감사 | `scripts/check-final-git-state.mjs` | stale push metadata 검사, `--finalize` clean/sync 검사 추가 |
| 감사 연결 | `scripts/audit-harness.mjs`, `package.json` | `audit:harness`에 push metadata 검사 연결, `audit:finalize` 추가 |
| 원장 보정 | `logs/tasks/TASK-20260429-010.jsonl`, `logs/tasks/TASK-20260429-011.jsonl` | `push_status=PUSHED` CORRECTION append |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check scripts/check-final-git-state.mjs` | PASS |
| `npm run audit:harness` | PASS |
| `node scripts/check-final-git-state.mjs` | PASS |
| `npm run check:encoding` | PASS |
| `git diff --check` | PASS |
| `npm run audit:finalize` | EXPECTED_FAIL on task branch dirty state |

`audit:finalize` 실패는 의도한 동작이다. 최종 검사는 main에서 clean worktree와 `main...origin/main` 동기화를 확인해야 한다.

## 품질 점수

95점 / 100점 (S등급)

감점/주의:
- 실제 모델 smoke test는 이 Task에 섞지 않고 별도 TASK-015로 분리했다.
- 과거 전체 ledger의 stale push 표기를 한 번에 강제하지 않고 TASK-20260429-010 이후로 제한했다. 레거시 일괄 보정은 별도 정책 Task가 필요하다.
