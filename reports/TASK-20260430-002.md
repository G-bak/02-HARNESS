# TASK-20260430-002 보고서 — Validator-B 파이프라인 하드닝

## 0. Executive Summary

- **상태:** COMPLETE
- **결론:** Claude 리뷰에서 지적된 Validator-B 파이프라인 결함을 코드·감사·fixture·가이드까지 반영해 수정했다.
- **서비스 영향:** 제품 런타임 영향 없음. 하네스 운영 자동화만 변경.
- **검증:** Validator-B dry-run PASS, workspace-write negative guard PASS, `npm run audit:harness` PASS, Validator-A 3차 PASS.
- **남은 리스크:** Gemini CLI 미설치로 live model smoke는 여전히 별도 Task가 필요.
- **배포:** main squash commit `ce886ab` 반영, post-completion record 후 push.
- **조치 필요:** Gemini CLI 설치·인증 후 live stdout envelope 확인.

## 1. 수정 내용

- `run-validator-b`가 전체 prompt를 argv에 싣지 않고 stdin으로 전달하도록 수정했다.
- Validator-B sandbox label은 `read-only`만 허용하고, Gemini CLI에는 boolean `--sandbox`만 전달하도록 명확히 했다.
- changed file context에 전체 1 MiB budget을 추가하고 초과 파일은 `omitted_for_total_budget`으로 표시하게 했다.
- Validator-B handoff 독립성 검사를 `refs`, `changed_files`, `previous_failures`의 `validator-a`/`codex` 참조까지 확장했다.
- Validator-B prompt에 `docs/schemas/validator-result.schema.json` 원문을 포함해 schema contract를 강화했다.
- Validator-A result 검증도 `github_commit`, `tier_reclassification_reason` 필수 필드를 검사하도록 맞췄다.
- `validate-handoffs`의 Validator-B fixture 검사를 단일 Task 경로 하드코딩에서 glob discovery로 바꿨다.
- `tasks/fixtures/validator-b-input.json`를 추가했다.

## 2. 검증

| 항목 | 결과 |
|---|---|
| `node --check scripts/run-validator-b.mjs` | PASS |
| `node --check scripts/run-validator-a.mjs` | PASS |
| `node --check scripts/validate-handoffs.mjs` | PASS |
| Validator-B dry-run | PASS |
| Validator-B `--sandbox workspace-write` negative guard | PASS |
| `npm run validate:handoffs` | PASS |
| `npm run audit:harness` | PASS |
| `git diff --check` | PASS |
| Validator-A attempt 3 | PASS |

Validator-A attempt 2는 `--sandbox read-only` 값을 기대해 FAIL했지만, Gemini CLI의 `--sandbox`는 값 없는 enable flag이므로 코드가 아니라 기준 설명을 보정했다. 가이드와 validation evidence에 “02-HARNESS의 read-only 라벨은 wrapper가 강제하고 Gemini CLI에는 boolean `--sandbox`만 넘긴다”는 내용을 반영한 뒤 Validator-A 3차 PASS를 받았다.

## 3. 품질 점수

**96점 / 100점 (S등급)**

좋았던 점: 리뷰의 HIGH/MEDIUM/LOW 중 코드로 닫을 수 있는 항목을 runner, audit, fixture, docs까지 연결해 회귀 방지했다. Validator-A가 잡은 모호한 sandbox 기준도 공식 CLI 의미에 맞춰 정리했다.

감점/주의: Gemini CLI live smoke는 아직 환경 미설치로 남아 있다.

## 4. 다음 권장 작업

1. Gemini CLI 설치·인증 후 live `run-validator-b` smoke test.
2. live stdout envelope가 현재 extractor와 다르면 `readGeminiResult`를 실측 형태에 맞춰 단순화.
