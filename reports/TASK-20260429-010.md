# TASK-20260429-010 보고서 — Generator wrapper output parsing fix

## 핵심 요약

- **상태:** 완료. main pure squash merge 완료, post-completion 기록 및 push 단계 진행.
- **결론:** `scripts/run-generator.mjs`가 Claude CLI stdout의 `structured_output`을 우선 수용하고, 자연어 `result`만 있는 경우 target file git diff 검증으로 합성 `generator-result.json`을 만들 수 있게 수정했다.
- **서비스 영향:** 제품 런타임 영향 없음. 운영 wrapper와 운영 가이드, 감사 체크만 변경.
- **검증:** `node --check scripts/run-generator.mjs`, `node scripts/check-generator-output-parsing.mjs`, `npm run audit:harness`, `npm run check:encoding`, `git diff --check` PASS.
- **남은 리스크:** 자연어 fallback은 git diff 기반이므로 파일 변경을 동반하지 않는 산출물에는 `HOLD`가 유지된다. 이 동작은 의도된 안전 장치다.
- **조치 필요:** post-completion 기록 커밋 후 push 및 브랜치 정리.

## 변경 사항

| 파일 | 변경 |
|---|---|
| `scripts/run-generator.mjs` | `structured_output` 우선 처리, 자연어 fallback 합성 결과, diff summary 기록, parser 함수 export |
| `scripts/check-generator-output-parsing.mjs` | structured output 및 자연어 fallback fixture 검증 추가 |
| `scripts/audit-harness.mjs` | 새 parsing check를 harness audit에 포함 |
| `docs/guides/generator-pipeline-ko.md` | 자연어 fallback 운영 절차와 HOLD 조건 문서화 |
| `tasks/handoffs/TASK-20260429-009/generator-result.json` | 기존 raw stdout의 `structured_output`으로 복구 산출물 재구성 |

## 검증

| 명령 | 결과 |
|---|---|
| `node --check scripts/run-generator.mjs` | PASS |
| `node scripts/check-generator-output-parsing.mjs` | PASS |
| `npm run audit:harness` | PASS |
| `npm run check:encoding` | PASS |
| `git diff --check` | PASS |

## 품질 점수

96점 / 100점 (S등급)

| 구분 | 점수 |
|---|---:|
| Outcome | 58 / 60 |
| Process | 38 / 40 |
| **합계** | **96 / 100** |

좋았던 점:
- Claude stdout wrapper의 실제 형태(`result` 자연어 + `structured_output`)를 반영해 파서를 보강했다.
- 자연어 fallback은 diff 검증 없이는 성공 처리하지 않도록 제한했다.
- TASK-009 누락 산출물을 provenance와 함께 복구했다.

감점/주의:
- wrapper 전체를 실제 Claude subprocess로 재호출하는 통합 테스트는 비용과 외부 auth 의존성 때문에 수행하지 않았다.
- 자연어 fallback은 파일 변경형 Generator 작업에만 적합하다.

## 인사이트 캡처

신규 재사용 인사이트 없음. TASK-009에서 이미 `--json-schema` 한계와 운영 gotcha가 기록되어 이번 Task는 구현 반영에 집중했다.
