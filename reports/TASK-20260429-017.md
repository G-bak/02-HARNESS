# TASK-20260429-017 보고서 — Validator-A 실제 smoke 재검증

## 핵심 요약

- **상태:** PASS.
- **결론:** TASK-016의 Windows `codex.cmd` shim 보강은 유효했다. 실제 Codex Validator-A 호출이 완료됐고 Validator-A가 PASS를 반환했다.
- **서비스 영향:** 제품 런타임 영향 없음. 하네스 wrapper/schema/가이드와 smoke artifact만 변경.
- **검증:** `run-validator-a.mjs` attempt 3에서 exit code 0, `validator-a-result-1.json` 생성, 모든 success criteria PASS.
- **남은 리스크:** Codex stderr에 Windows sandbox `CreateProcessWithLogonW failed: 1056` 로그가 남았지만 최종 Validator 판정은 PASS였다. 추후 반복되면 sandbox 세부 옵션을 별도 조사한다.
- **조치:** 이번 Task에서 `-a never` 옵션 순서와 structured-output schema strictness를 wrapper/schema/가이드에 즉시 반영했다.

## 실행 결과

| 단계 | 결과 | 근거 |
|---|---|---|
| attempt 1 | Resource Failure | `codex exec ... -a never` 순서가 현재 Codex CLI에서 `unexpected argument '-a'` |
| attempt 2 | Resource Failure | CLI 호출은 진행됐지만 `validator-result.schema.json`이 structured-output strict schema 요구를 충족하지 못함 |
| attempt 3 | PASS | `codex -a never exec ...` + strict schema 보강 후 `Validator-A PASS` |

## Validator-A 판정

Validator-A는 아래 항목을 모두 PASS로 판정했다.

| 기준 | 결과 |
|---|---|
| smoke target의 정확한 문구 존재 | PASS |
| Generator result status가 `PENDING_VALIDATION` | PASS |
| artifact list에 smoke target 포함 | PASS |
| smoke target/result에 secret, credential, PII, env 값 없음 | PASS |
| Resource Failure를 Validator FAIL로 취급하지 않음 | PASS |

## 발견 및 반영

1. 현재 Codex CLI에서 승인 정책 `-a never`는 `exec` 하위 명령 뒤가 아니라 최상위 `codex` 옵션 위치에 있어야 한다. `scripts/run-validator-a.mjs`와 `docs/guides/validator-pipeline-ko.md`에 반영했다.
2. Codex structured output schema는 object마다 `additionalProperties: false`를 요구한다. `docs/schemas/validator-result.schema.json`을 strict 형태로 보강했다.

## 품질 점수

94점 / 100점 (S등급)

감점/주의:
- 실제 smoke 중 두 번의 Resource Failure가 있었고, 그 과정에서 wrapper/schema 호환 결함을 추가로 고쳤다.
- stderr의 Windows sandbox 로그는 PASS를 막지는 않았지만 후속 관찰 대상이다.
