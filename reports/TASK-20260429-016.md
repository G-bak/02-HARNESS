# TASK-20260429-016 보고서 — 가이드 결함 자동 보강 및 wrapper 강제

## 핵심 요약

- **상태:** 구현 및 로컬 검증 완료.
- **결론:** TASK-015에서 드러난 두 결함을 보고서에만 남기지 않고 wrapper와 가이드에 반영했다. 추가로 앞으로 가이드/wrapper/process 결함을 `insight_capture=not_needed` + 보고서-only로 닫는 경로를 완료 게이트에서 막는다.
- **서비스 영향:** 제품 런타임 영향 없음. 하네스 운영 문서, wrapper, 감사 스크립트만 변경.
- **검증:** `node --check`, Generator fixture, `audit:harness`, `check:encoding`, `git diff --check`, Validator-A dry-run PASS.

## 변경 사항

| 영역 | 파일 | 내용 |
|---|---|---|
| Generator wrapper | `scripts/run-generator.mjs` | 실행 직전 워킹트리 baseline을 잡고 현재 Generator 실행에서 새로 생긴 변경분만 `target_files`와 대조 |
| Generator fixture | `scripts/check-generator-output-parsing.mjs` | setup 파일은 baseline으로 무시하고 smoke target만 current-run delta로 잡는 회귀 테스트 추가 |
| Validator wrapper | `scripts/run-validator-a.mjs` | Windows `.cmd`/`.bat` shim을 `cmd.exe /d /s /c` 경유로 실행하고 `actual_spawn_command` 기록 |
| 완료 게이트 | `scripts/check-completion-gates.mjs` | 반복 가능한 가이드/wrapper/process 결함을 보고서-only로 닫는 경우 차단 |
| 가이드 | `docs/guides/generator-pipeline-ko.md` | `main...HEAD` 기준이 아니라 pre-run baseline 기준임을 문서화 |
| 가이드 | `docs/guides/validator-pipeline-ko.md` | Windows Codex shim gotcha와 metadata 확인 지점 문서화 |
| 정책 | `docs/operations/work-history-policy.md`, `docs/operations/eval-harness.md` | 가이드 결함은 인사이트·문서·스크립트·후속 Task 중 하나로 남겨야 한다고 명시 |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check scripts/run-generator.mjs` | PASS |
| `node --check scripts/run-validator-a.mjs` | PASS |
| `node --check scripts/check-completion-gates.mjs` | PASS |
| `node scripts/check-generator-output-parsing.mjs` | PASS |
| `npm run audit:harness` | PASS |
| `npm run check:encoding` | PASS |
| `git diff --check` | PASS |
| `npm run run:validator-a -- TASK-20260429-015 --input tasks/handoffs/TASK-20260429-015/validator-a-input.json --codex-bin codex.cmd --attempt 4 --dry-run` | PASS |

## 남은 범위

이 Task는 wrapper와 감사 보강이다. 실제 Codex 모델까지 다시 호출하는 smoke 재시도는 별도 Task로 분리해야 한다. 이 Task에서는 live Validator-A 모델 실행을 수행하지 않았다.

## 품질 점수

96점 / 100점 (S등급)

감점/주의:
- Windows `.cmd` shim 경로는 dry-run과 코드 검증으로 확인했고, live Codex 모델 재호출은 하지 않았다.
- 완료 게이트의 자연어 패턴은 최소 방어선이다. 모든 보고서 표현을 완벽히 해석하지 않으므로 인사이트 기록 책임은 계속 Analyst에게 있다.
