# TASK-20260430-005 보고서 — Validator-B 파이프라인 하드닝

## 통합 요약

- **상태:** PENDING MERGE
- **결론:** Claude/Codex 합동 리뷰의 HIGH/MEDIUM 지적을 코드, 감사, 문서, 실측 negative smoke까지 반영했다.
- **서비스 영향:** 제품 런타임 영향 없음. 하네스 Validator runner와 운영 문서만 변경.
- **검증:** `node --check` PASS, `check-validator-b-regressions` PASS, live Gemini negative smoke 기대 FAIL 확인, `npm run audit:harness` PASS, `git diff --check` PASS, Validator-A attempt 3 PASS.
- **남은 리스크:** 없음. main squash merge만 남음.
- **조치 필요:** main squash merge, push, branch cleanup.

## 변경 요약

| 영역 | 처리 |
|---|---|
| event_id 충돌 | `scripts/lib/ledger-events.mjs`로 max-suffix 기반 event_id 할당을 공통화하고 Validator-A/B runner에 적용 |
| 원장 감사 | `validate-ledger.mjs`가 중복 event_id를 실패 처리하도록 강화. legacy 중복은 append-only CORRECTION으로 명시 |
| malformed Gemini 출력 | exit 0이어도 JSON/schema-invalid 출력이면 `MALFORMED_OUTPUT` Resource Failure 기록 |
| 독립성 | Validator-B prompt에 validator-a/codex artifact 금지 지시 추가, 임베드 파일 내용의 validator-a/codex 참조 차단 |
| approval metadata | Validator-B 원장/metadata의 `approval_policy=never`를 실제 CLI 의미인 `approval_mode=plan`으로 정정 |
| Tier 정책 | Validator-B는 Tier 3 전용, 명시적 runner smoke만 Tier 2 예외 허용 |
| 회귀 테스트 | `check-validator-b-regressions.mjs`를 audit:harness에 편입 |
| prompt artifact | 신규 `validator-b-prompt-*.txt`는 `.gitignore`로 기본 제외 |

## 검증 결과

| 항목 | 결과 |
|---|---|
| `node --check` changed scripts | PASS |
| `node scripts/check-validator-b-regressions.mjs` | PASS |
| live Gemini negative smoke | PASS: 의도적 결함에 대해 Validator-B가 `FAIL` + `SPEC_MISMATCH` 반환 |
| `npm run audit:harness` | PASS |
| `git diff --check` | PASS |
| Validator-A attempt 1 | FAIL: 파일 검토 전 조기 JSON 반환 |
| Validator-A attempt 2 | FAIL: 보고서/품질 점수/validation-evidence 누락 및 node/npm 실행 제한 지적 |
| Validator-A attempt 3 | PASS |

## 에이전트별 활동 이력

### Generator

- 실제 호출 여부: 아니오. Analyst가 사용자 지시와 기존 리뷰를 바탕으로 직접 수정했다.
- 산출물: runner, 감사 스크립트, 문서, negative smoke handoff, 검증 evidence.

### Validator-B

- 실제 호출 여부: 예.
- 실행: `gemini.cmd --prompt <prompt omitted> --output-format json --approval-mode plan`
- substance: 결함 있는 `negative-generator-result.json`을 읽고 `negative_smoke_output` 누락을 `SPEC_MISMATCH`로 판정했다. 이전 TASK-003의 vacuous smoke 한계를 보완했다.

### Validator-A

- 실제 호출 여부: 예, attempt 1/2/3.
- substance: attempt 1은 파일 검토 없이 self-fail했고, wrapper 지시문을 강화했다. attempt 2는 파일 검토 후 보고서/품질 점수/validation-evidence 누락과 read-only 런타임의 node/npm 실행 제한을 지적했다. attempt 3은 파일 검토와 validation-evidence 보조 확인 후 PASS를 반환했다.

### Analyst

- Task spec/ledger 생성, 코드·문서 보정, local validation, live negative smoke, 보고서/품질 점수 작성, Validator-A 재검증 준비를 수행했다.

## 품질 점수

**96점 / 100점 (S등급)**

좋았던 점: 단순 문서 반영이 아니라 event_id 충돌, malformed output, negative smoke, 독립성 지시, audit gate까지 실제 재발 방지 장치로 닫았다.

감점/주의: Validator-A read-only 런타임에서 node/npm 실행이 제한되어 parent-session validation evidence를 보조 근거로 연결해야 했다.

## 다음 단계

1. task branch 커밋.
2. main squash merge, push, branch cleanup.
