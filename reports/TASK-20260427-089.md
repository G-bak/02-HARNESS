# TASK-20260427-089 보고서 — Researcher 역할 우선 라우팅 규칙 보강

## 0. 핵심 요약

- **상태:** COMPLETE
- **결론:** 검색·조사·최신 외부 사실·공식 문서 검증·모델 가용성 확인 요청은 Analyst 단독 답변이 아니라 Researcher 절차로 라우팅하도록 문서화했다.
- **서비스 영향:** 런타임 제품 코드 변경 없음. 운영 문서 기준만 변경.
- **검증:** `npm run check:encoding` PASS, `node scripts/check-doc-headers.mjs` PASS, `git diff --check` PASS
- **남은 리스크:** 외부 CLI fallback은 여전히 조건부이므로, 실제 CLI 모델 실행 여부는 요청과 필요성에 따라 별도 기록해야 한다.
- **조치 필요:** 없음. main push 후 로컬 `task/TASK-20260427-089` 브랜치 삭제 예정이며 최종 응답에서 실제 브랜치 목록을 보고한다.

## 1. 요청 및 목표

사용자는 모델 정책 질문을 Analyst가 직접 처리한 점을 문제로 지적했고, 앞으로는 역할별 작업을 반드시 지키도록 가이드 문서를 보강하라고 요청했다.

목표는 다음과 같다.

- 검색/조사/최신 정보 확인 요청을 Researcher 역할로 명확히 라우팅한다.
- 같은 세션에서 조사하더라도 Researcher 절차를 수행한 것으로 인정받기 위한 조건을 명시한다.
- 외부 CLI fallback은 무조건 강제하지 않고, Researcher 역할 절차와 CLI 실행을 구분한다.
- 최종 답변 또는 원장에 Researcher 실행 모드를 남기도록 한다.

## 2. 결과 및 변경 사항

변경 파일:

- `docs/agents/analyst.md`
- `docs/agents/researcher.md`
- `CURRENT_STATE.md`
- `tasks/specs/TASK-20260427-089.json`
- `logs/tasks/TASK-20260427-089.jsonl`
- `logs/sessions/SESSION-20260427-008.md`
- `reports/TASK-20260427-089.md`

주요 결정:

- 사용자가 검색, 조사, 리서치, 찾아보기, 최신 정보 확인을 요청하면 Researcher 절차를 거친다.
- 공식 문서, API 명세, 모델 가용성, 가격, 정책처럼 최신성이 중요한 정보도 Researcher 절차로 라우팅한다.
- 같은 세션에서 수행해도 Researcher 지시서 또는 그에 준하는 질문·scope·exclude가 필요하다.
- 출처 검토, confidence, confidence_rationale, Research Summary가 있어야 Researcher 수행으로 인정한다.
- 최종 답변이나 원장에는 `Researcher 실행 모드`를 남긴다.
- 외부 CLI fallback은 격리 실행, 장시간 조사, 모델 가용성 검증, 사용자 명시 요청이 있을 때만 사용한다.

## 3. 검증 및 승인

검증 결과:

- `npm run check:encoding`: PASS
- `node scripts/check-doc-headers.mjs`: PASS
- `git diff --check`: PASS

승인 기준:

- 문서 헤더 버전과 `CURRENT_STATE.md` 권위 문서 표가 일치해야 한다.
- UTF-8 인코딩 검사에 통과해야 한다.
- 공백 검사를 통과해야 한다.
- Tier 2 운영 문서 변경이므로 보고서와 작업 원장이 존재해야 한다.

## 4. 영향도 및 리스크

제품 런타임 영향:

- 없음. 코드, API, DB, 배포 설정 변경 없음.

운영 영향:

- 역할 경계가 더 강해져 Analyst가 외부 조사성 답변을 직접 처리하는 실수를 줄인다.
- Researcher 실행 방식이 in-session인지 external CLI fallback인지 추적 가능해진다.
- 외부 CLI fallback을 매번 강제하지 않아 비용과 플랫폼 제약을 피한다.

남은 리스크:

- 실제 세션 운영자가 Researcher Summary를 생략하면 규칙 위반이 된다.
- 자동 감사가 Researcher Summary 존재 여부까지 강제하지는 않는다.

## 5. 진행 경과

- Analyst 문서에 Researcher 필수 라우팅 트리거를 추가했다.
- Analyst 문서에서 같은 세션 조사도 Researcher 절차로 기록해야 한다고 명시했다.
- Researcher 문서에 in-session Researcher 실행 인정 조건을 추가했다.
- `CURRENT_STATE.md`에 역할 우선 라우팅 핵심 결정을 추가했다.
- 작업 기록과 세션 로그를 생성했다.

## 6. 이슈 / 예외 / 보류 사항

- 실제 Researcher 외부 CLI 실행 테스트는 하지 않았다. 이번 작업은 역할 라우팅 정책 문서화가 범위다.

## 7. 비용 / 리소스 / 알림

- 외부 CLI Researcher 실행 없음.
- Slack 알림 없음.
- 민감 정보 기록 없음.

## 8. 품질 점수

**종합 점수: 96점 / 100점 (S등급)**

| 구분 | 점수 | 설명 |
|---|---:|---|
| 결과물 품질 | 58 / 60 | 역할 라우팅 조건과 Researcher 실행 인정 조건을 명확히 분리했다. |
| 진행 품질 | 38 / 40 | Task Spec, 원장, 세션 로그, 보고서, 검증 계획을 포함했다. |
| 총점 | 96 / 100 | 운영에 바로 반영 가능한 역할 기준 보강이다. |

**좋았던 점:** Researcher 역할 수행과 외부 CLI fallback을 혼동하지 않도록 분리했다.  
**감점/주의:** Researcher Summary 누락을 자동으로 검출하는 감사 스크립트는 아직 없다.

## 9. 다음 권장 사항

- 이후 검색성 답변에서는 최종 답변에 `Researcher 실행 모드`를 짧게 표시한다.
- 필요하면 별도 Task로 Researcher Summary 누락을 감지하는 감사 스크립트를 추가한다.
