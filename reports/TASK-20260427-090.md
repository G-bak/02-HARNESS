# TASK-20260427-090 보고서 — 레거시 원장 감사 실패 보정

## 0. 핵심 요약

- **상태:** COMPLETE
- **결론:** 기존 `audit:harness` 실패 원인을 원장 구조화 필드 누락과 JSONL 파싱 손상으로 분리해 보정했고, 전체 감사가 PASS로 전환됐다.
- **서비스 영향:** 제품 런타임 코드 변경 없음. 운영 원장과 작업 이력 가이드만 변경.
- **검증:** `npm run audit:harness` PASS, `git diff --check` PASS, 대상 JSONL 파싱 PASS
- **남은 리스크:** 레거시 원장 보정은 과거 사실을 재구성한 것이므로, 새 작업에서는 `CORRECTION`이 아니라 최초 완료 이벤트에 구조화 필드를 직접 남겨야 한다.
- **조치 필요:** 없음.

## 1. 원인 분석

감사 실패는 세 단계로 드러났다.

1. `validate-ledger` 단계
   - `TASK-20260426-062`~`066`, `068`, `072`: `details.session_log`는 있었지만 `artifact_refs`에 `type=session` 참조가 없었다.
   - `TASK-20260426-067`: 첫 줄 끝에 `}`가 하나 더 있어 JSONL 파서가 `TASK_CREATED`를 읽지 못했다.

2. `check-completion-gates` 단계
   - `TASK-20260426-080`: Tier 2 완료지만 구조화된 `quality_score`와 `VALIDATION_RESULT` 또는 omission 사유가 없었다.
   - `TASK-20260427-084`~`089`: 검증/품질/머지 정보가 자연어 또는 배열 필드에는 있었지만, 완료 게이트가 읽는 구조화 필드가 부족했다.

3. 재발 원인
   - 사람에게 읽히는 기록과 자동 감사가 읽는 구조화 필드를 혼동했다.
   - 완료 직후 해당 `TASK-*.jsonl`을 JSONL 파서로 검증하는 절차가 강제되지 않았다.
   - Tier 2/3 완료 게이트 필드(`VALIDATION_RESULT`, `MERGE_COMPLETED`, `quality_score`)가 보고서 문장이나 배열로 대체될 수 있다고 오판했다.

## 2. 변경 사항

원장 보정:

- `TASK-20260426-062`~`066`, `068`, `072`에 `CORRECTION.artifact_refs`로 세션 로그 참조를 추가했다.
- `TASK-20260426-067`은 첫 줄의 추가 `}` 1개를 제거해 원본 `TASK_CREATED`를 복구했고, 보정 사유와 완료 이벤트를 남겼다.
- `TASK-20260426-080`, `TASK-20260427-084`~`089`에 구조화된 품질 점수, 검증 omission 사유, 머지 omission 사유를 `CORRECTION.details`로 보강했다.

가이드 보강:

- `docs/operations/work-history-policy.md`를 v1.12로 올렸다.
- 세션 로그는 `details.session_log`가 아니라 `artifact_refs`에도 남겨야 한다고 명시했다.
- Tier 2/3 완료에는 `VALIDATION_RESULT`, `MERGE_COMPLETED`, 구조화된 `quality_score`가 필요하다고 명시했다.
- JSONL 파싱 실패는 후속 append만으로 해결할 수 없으며, 최소 문자 수정과 `CORRECTION` 기록이 필요하다고 명시했다.

## 3. 검증

- `npm run audit:harness`: PASS
- `node scripts/check-doc-headers.mjs`: PASS
- `node scripts/validate-ledger.mjs`: PASS
- `node scripts/check-completion-gates.mjs`: PASS
- `node scripts/check-quality-scores.mjs`: PASS
- `git diff --check`: PASS
- 대상 JSONL 파싱 확인: PASS

## 4. 품질 점수

**종합 점수: 97점 / 100점 (S등급)**

| 구분 | 점수 | 설명 |
|---|---:|---|
| 결과물 품질 | 59 / 60 | 감사 실패 전체를 원인별로 분리하고 모든 게이트를 PASS로 만들었다. |
| 진행 품질 | 38 / 40 | 원장, 세션 로그, 보고서, 품질 점수, 검증을 남겼다. |
| 총점 | 97 / 100 | 레거시 보정과 재발 방지 문서화가 함께 완료됐다. |

**좋았던 점:** 기존 완료 사실을 덮어쓰지 않고 `CORRECTION` 중심으로 감사 필드만 보강했다.  
**감점/주의:** 레거시 `TASK-20260426-067`은 파싱 자체가 불가능해 최소 문자 수정이 필요했다.

## 5. 다음 운영 규칙

- 새 완료 이벤트에는 사람이 읽는 요약과 자동 감사용 구조화 필드를 모두 남긴다.
- 세션 로그는 `artifact_refs`에 반드시 연결한다.
- Tier 2/3는 `VALIDATION_RESULT`, `MERGE_COMPLETED`, `quality_score`를 생략하지 않는다.
- `TASK_COMPLETED` 직후 JSONL 파싱과 `npm run audit:harness`를 확인한다.
