# TASK-20260427-088 보고서 — Researcher 모델 선택 정책 문서화

## 0. 핵심 요약

- **상태:** COMPLETE
- **결론:** Researcher 외부 CLI fallback 모델은 단일 고정값이 아니라 작업 위험도·비용·지원 여부 기준으로 선택하도록 문서화했다.
- **서비스 영향:** 런타임 제품 코드 변경 없음. 운영 문서 기준만 변경.
- **검증:** `npm run check:encoding` PASS, `node scripts/check-doc-headers.mjs` PASS, `git diff --check` PASS
- **남은 리스크:** 실제 CLI에서 `gpt-5.5` 지원 여부는 계정/실행 환경별로 확인해야 한다.
- **조치 필요:** 없음. main push 후 로컬 `task/TASK-20260427-088` 브랜치 삭제 예정이며 최종 응답에서 실제 브랜치 목록을 보고한다.

## 1. 요청 및 목표

사용자는 이번 세션에서 논의한 Researcher 모델 선택 기준이 세션 종료 후 사라지지 않도록 가이드 문서에 남기라고 요청했다.

목표는 다음과 같다.

- Researcher가 항상 `gpt-5.4`만 쓰는 것으로 굳지 않게 한다.
- `gpt-5.5` 같은 최신 모델은 지원될 때 고가치 조사에 우선 사용하도록 한다.
- 단순 조회는 경량 모델 또는 현재 세션 모델을 허용해 비용을 줄인다.
- 최신 모델 미지원 시 `gpt-5.4` fallback과 기록 의무를 명확히 한다.

## 2. 결과 및 변경 사항

변경 파일:

- `docs/agents/researcher.md`
- `docs/agents/analyst.md`
- `CURRENT_STATE.md`
- `tasks/specs/TASK-20260427-088.json`
- `logs/tasks/TASK-20260427-088.jsonl`
- `logs/sessions/SESSION-20260427-007.md`
- `reports/TASK-20260427-088.md`

주요 결정:

- Researcher 모델 선택은 조사 유형별로 세분화한다.
- 단순 사실 확인은 `gpt-5.4-mini` 또는 현재 세션 모델을 허용한다.
- 일반 외부 기술 조사와 여러 출처 충돌 판정은 최신 지원 flagship 모델을 우선 사용한다.
- `gpt-5.5`가 실행 CLI/account에서 지원되면 우선 사용하고, 미지원이면 사유를 기록한 뒤 `gpt-5.4`로 fallback한다.
- 장시간 고난도 조사에서는 지원될 때 `gpt-5.5-pro` 같은 advanced/pro 모델을 선택할 수 있다.
- Researcher 외부 CLI fallback 실행 시 사용 모델, fallback 여부, command, sandbox를 원장 또는 보고서에 기록한다.

## 3. 검증 및 승인

검증 결과:

- `npm run check:encoding`: PASS
- `node scripts/check-doc-headers.mjs`: PASS
- `git diff --check`: PASS

승인 기준:

- 문서 헤더 버전과 `CURRENT_STATE.md` 권위 문서 표가 일치해야 한다.
- UTF-8 인코딩 검사에 통과해야 한다.
- Tier 2 운영 문서 변경이므로 보고서와 작업 원장이 존재해야 한다.

## 4. 영향도 및 리스크

제품 런타임 영향:

- 없음. 코드, API, DB, 배포 설정 변경 없음.

운영 영향:

- Researcher 모델 선택이 비용과 정확도 사이에서 더 현실적으로 운영된다.
- 최신 모델이 지원될 때는 고위험 조사 품질을 높일 수 있다.
- 미지원 모델을 무조건 실행하는 실수를 줄인다.

남은 리스크:

- 모델 제공 여부는 시간과 계정 상태에 따라 바뀔 수 있다.
- 문서의 모델 이름은 정책 기준이며, 실제 실행 전 CLI/account 지원 여부를 확인해야 한다.

## 5. 진행 경과

- 공식 문서 기준으로 최신 모델 우선 원칙을 확인했다.
- Researcher 문서에 모델 선택 기준 표와 fallback 규칙을 추가했다.
- Analyst 문서의 외부 CLI fallback 예시와 모델 선택표를 최신 지원 모델 우선 기준으로 바꿨다.
- `CURRENT_STATE.md`에 Researcher 모델 선택 핵심 결정을 추가했다.
- 작업 기록과 세션 로그를 생성했다.

## 6. 이슈 / 예외 / 보류 사항

- 실제 `codex exec -m gpt-5.5` 실행 테스트는 하지 않았다. 이번 작업은 정책 문서화가 범위다.

## 7. 비용 / 리소스 / 알림

- 외부 CLI Researcher 실행 없음.
- Slack 알림 없음.
- 민감 정보 기록 없음.

## 8. 품질 점수

**종합 점수: 95점 / 100점 (S등급)**

| 구분 | 점수 | 설명 |
|---|---:|---|
| 결과물 품질 | 57 / 60 | 최신 모델 우선, 경량 모델, fallback 기록 기준을 분리했다. |
| 진행 품질 | 38 / 40 | Task Spec, 원장, 세션 로그, 보고서, 검증 계획을 포함했다. |
| 총점 | 95 / 100 | 운영에 바로 반영 가능한 정책 문서화다. |

**좋았던 점:** 단일 모델 고정 대신 비용·정확도·지원 여부를 함께 고려했다.  
**감점/주의:** 실제 CLI 모델 지원 테스트는 별도 Task가 필요하다.

## 9. 다음 권장 사항

- 실제 Researcher 외부 CLI를 처음 사용할 때 `gpt-5.5` 지원 여부를 원장에 기록한다.
- 모델 지원 상태가 바뀌면 `docs/agents/researcher.md`의 모델 선택표를 갱신한다.
